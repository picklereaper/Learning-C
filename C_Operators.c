#include<stdio.h>

int main ()
{
    //Arithmetic Operators, basic operators: +, -, /, *, %(Remainder), ++(Incrimate by 1), --(decrement by 1)
    int x = 12;

    // int result = x +8;  Both ways work, but this one is better for large projects or if you need to change the variable.
    


    printf("%d", x+8);



    return 0;
}